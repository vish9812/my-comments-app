{
  "configurations": [
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/src/server/package.json",
        "localRoot": "${workspaceFolder}/src/server"
      }
    }
  ]
}
